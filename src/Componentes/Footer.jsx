// Footer.jsx
// Este é o nosso componente para o rodapé da página.

function Footer() {
    // O return define o conteúdo visual do rodapé.
    return (
      <footer>
        <hr /> {/* Linha horizontal para separar visualmente */}
        <p>
          Disciplina: ADS 2MA - Professor Renato Freire
        </p>
      </footer>
    );
  }
  
  // Exportamos o componente para que ele possa ser usado em outros arquivos.
  export default Footer;